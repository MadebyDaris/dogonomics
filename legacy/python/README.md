# Legacy Python Archive

This folder contains Python artifacts that are no longer part of the active backend workflow.

Active backend workflow is Go + PowerShell scripts under `scripts/`.
Use `scripts/dev.ps1` as the main command entrypoint.
