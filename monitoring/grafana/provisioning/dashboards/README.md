Place dashboard JSON files into this folder to auto-provision dashboards. You can export dashboards from Grafana and put them here. Grafana will pick them up on startup if provisioning is configured.
