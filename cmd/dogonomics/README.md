# cmd/dogonomics

Target entrypoint location for the backend application during the project-layout migration.

Current runtime entrypoint is still at backend root (`dogonomics.go`) until Phase 2 migration is completed.
